\name{s.dc.outlier_detector}
\alias{hello}
\title{Detection of outliers}
\usage{
s.dc.outlier_detector(dataset, ID_name = 'ID', sig_num = 3, NA_obs_out = FALSE, in_list = NULL, out_list = NULL)
}
\description{
1. 針對變數(直欄)列出是否有離群值
2. 針對變數列出是否有遺漏值
}
\examples{
hello()
}
